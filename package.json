{
  "name": "rapidito",
  "private": true,
  "scripts": {
    "setup": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend && npm run dev",
    "start": "./start.sh",
    "lint": "cd backend && python3 -m py_compile main.py auth.py schemas.py && python3 -m py_compile routers/*.py && cd ../frontend && npx tsc --noEmit"
  }
}
